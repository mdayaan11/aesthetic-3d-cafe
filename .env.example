# Database connection string for Prisma
DATABASE_URL="postgresql://user:password@localhost:5432/aesthetic_cafe?schema=public"

# Secret key for authentication (e.g., JWT signing)
AUTH_process.env.SECRET_REDACTED

# Base URL for the application (e.g., for absolute URLs in API calls or redirects)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
# NEXT_PUBLIC_APP_URL="https://your-production-domain.com"

# Node environment (development, production, test)
# NODE_ENV="development"